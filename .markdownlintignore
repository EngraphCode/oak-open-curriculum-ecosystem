# Markdownlint scope: lint canon surfaces inside `.agent/`, ignore the rest.
#
# Doctrine: `.agent/rules/no-warning-toleration.md` §Scope discipline.
# The gate's footprint IS the doctrine's footprint. Canon surfaces
# (directives, memory, plans, practice-context, practice-core, rules,
# skills, commands, sub-agents) lint by default. Reference / synthesis /
# archive / generated material is explicitly excluded below; moving a
# folder out of this list is a deliberate governance act, not a
# silent expansion.

# `.agent/` non-canon surfaces — explicitly excluded:
.agent/analysis/
.agent/archive/
.agent/evaluations/
.agent/experience/
.agent/hooks/
.agent/milestones/
.agent/proposals/
.agent/prompts/
.agent/reference/
.agent/reference-local/
.agent/reports/
.agent/research/
.agent/roles/

# Other agent-platform dot-dirs — out of scope for now (vendored third-party
# skill packs dominate; bringing them in is a separate governance act
# requiring per-folder vendored-content exclusions). The npm script passes
# `--dot` so these would otherwise be walked.
.cursor/
.claude/
.agents/

# Repo-wide non-canon surfaces:
tmp/
CHANGELOG.md
**/archive/**
**/reference/**
**/research/**
**/temp-secrets/**

# Third-party dependencies
node_modules/**
**/node_modules/**

# Generated
packages/sdks/oak-curriculum-sdk/docs/api-md/**
packages/sdks/oak-curriculum-sdk/docs/api/**
# Generated by `comms render` from comms/events/*.json bodies. Body
# content is authored as JSON strings; markdown formatting in those
# bodies is rendered verbatim and may not satisfy MD032 / MD031 (lists
# need blank lines around them). The renderer is the gate for the
# format; the file is the cached render output.
.agent/state/collaboration/shared-comms-log.md

# ARC AnGels (ArcAngel) rapid-comms channel logs: append-only, multi-author
# conversational turns — the same category as the rendered comms log above
# (chat-shaped content may not satisfy MD032/MD031, and the append-only
# convention forbids rewriting prior turns to fix formatting). The README
# mechanism doc in the same directory stays linted as canon; only the dated
# channel logs are excluded.
.agent/collaboration/rapid-comms/2026-*.md
.agent/collaboration/rapid-comms/.starless-notice-body.md

# Playwright test results
**/test-results/**
**/test-artifacts/**

# Stryker
.stryker-tmp
