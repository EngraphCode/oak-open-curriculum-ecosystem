# Sub-agent templates

Assembled sub-agent workflows composed from `../components/`. Conventions, dependency
rules, and the consistency checklist live in [`../README.md`](../README.md). Consumer
wrappers (for example `.claude/agents/<name>.md`) stay thin: frontmatter plus a
first-action instruction to read the template.
