<!-- User scratchpad: a starting statement for the fresh-seat rescued-knowledge
full-processing sessions (ws1c–ws1g). Not doctrine; a pasteable opener. -->

# Rescued-Knowledge Full Processing (ws1c–ws1g) — Session Opener

## Paste this

/oak-consolidate-until-done

You are opening a **dedicated knowledge-curation session**. The goal is the next
incomplete workstream of **ws1c–ws1g in
`.agent/plans/agentic-engineering-enhancements/current/corpus-analysis-salvage-and-topology-redesign.plan.md`**
(read its §Full-processing mandate first — it carries the owner direction, the
acceptance contract, and the method invariants; the plan todos are the
contract). This is disposition of already-paid-for discovery output — never
re-run any judgment stage.

### Ground (verify first-hand, do not skip)

Work on `feat/corpus_research_enhancements` (expect a clean tree; the plan
amendment that added ws1c–ws1g is in history). Register on comms (all-channels
watcher first — quote the display-name seen-file path; F-115 in the frictions
register explains why), open your claim, then work. Recompute every stratum
count at execution time (jq recipes are in the ws1c todo); counts in plan text
are derivation-anchored snapshots, never trusted.

### Standing corrections this work runs under

1. **Every item gets one of the four dispositions this pass** — graduate /
   enrich / duplicate / reject-with-named-reason. An entry hinging on an
   undecided design question routes INTO the owning artefact (the Phase-0
   agenda) as content; routing IS its disposition. The prior "owner-gated"
   framing on these strata was never owner-ratified and is withdrawn
   (owner direction 2026-07-03; `precedence-is-not-approval`).
2. **No fallbacks, ever** — F-112 is FIXED (`b2ae96898`); the commit-queue
   workflow is the only commit path; its failure is an error to stop on and
   surface. The commit-window claim's area pattern is `index/head` under kind
   `git` (the `git:` prefix is the kind label — the guard rejects a pattern
   spelled `git:index/head`).
3. **Critically assess all subagent output first-hand** before absorbing —
   findings, verdicts, "confirmed" claims, cited sources (owner standing
   directive; `verify-dont-trust`).
4. **Comms events are OUT of scope** — owner-routed to a separate session
   under a separate plan.
5. Conserve-by-default (PDR-122); the PDR-098 recurrence check applies with
   the temporal qualifier; no ledgers — homes and commits are the record.

### Cadence

One commit per batch (ws1d) or per workstream slice; batch boundary = the
natural mid-arc handoff boundary; napkin capture continuous; buffers empty or
explicitly dispositioned at each session close; closeout reports value and
impact, never accounting. zsh does not word-split unquoted variables (use
arrays); never `--no-verify`.
