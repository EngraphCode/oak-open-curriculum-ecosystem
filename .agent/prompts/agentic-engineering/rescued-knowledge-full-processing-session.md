<!-- User scratchpad: a starting statement for the fresh-seat rescued-knowledge
full-processing sessions (ws1d batches E-b2..E-b8 — the arc's sole remaining work).
Not doctrine; a pasteable opener. -->

> **CONSUMED — do not paste again.** The full-processing arc completed
> 2026-07-04 (Otter hunts Jetty): ws1d batches E-b2..E-b8 all executed, tier E
> 187/187 dispositioned, the ws1d todo completed, and the mandate's set-level
> acceptance verified in the plan status line. The successor work is the ws2
> topology readiness review, which folds into the corpus-generalisation
> Phase 0 design session (see the corpus-analysis-salvage-and-topology-redesign
> plan status and the agentic-engineering-enhancements thread record).

# Rescued-Knowledge Full Processing (ws1d E-b2..E-b8) — Session Opener

## Paste this

/oak-consolidate-until-done

You are opening a **dedicated knowledge-curation session**. The goal is the next
tier-E batch(es) of **ws1d in
`.agent/plans/agentic-engineering-enhancements/current/corpus-analysis-salvage-and-topology-redesign.plan.md`**
(read its §Full-processing mandate first — it carries the owner direction, the
acceptance contract, and the method invariants; the ws1d todo carries the drain
state and the E-b1 worked shape). Every other workstream of the arc (ws1b, ws1c,
ws1e, ws1f, ws1g, ws1d/E-b1) is EXECUTED — tier E ranks 26–187 (162 items) are
the arc's sole remaining work. This is disposition of already-paid-for discovery
output — never re-run any judgment stage.

### Ground (verify first-hand, do not skip)

Work on `feat/corpus_research_enhancements` (expect a clean tree at or after
`d79b56c63`). Register on comms (all-channels watcher first — quote the
display-name seen-file path; F-115), open your claim, then work. Recompute the
batch at execution time: `jq '.tierE[25:50]'` (E-b2) of
`data/discovery-run-salvage-tiers-2026-07-02.json` under
`.agent/reports/agentic-engineering/large-corpus-analysis-tooling/`; each next
batch advances the slice by 25. Counts in plan text are derivation-anchored
snapshots, never trusted.

### Standing corrections this work runs under

1. **Every item gets one of the four dispositions this pass** — graduate /
   enrich / duplicate / reject-with-named-reason. An entry hinging on an
   undecided design question routes INTO the owning artefact (the Phase-0
   agenda in the generalisation plan) as content; routing IS its disposition
   (owner direction 2026-07-03; `precedence-is-not-approval`).
2. **No fallbacks, ever** — the commit-queue workflow is the only commit path
   (F-112 fixed at `b2ae96898`); its failure is an error to stop on and
   surface. The commit-window claim's area pattern is `index/head` under kind
   `git` (F-116: the `git:` prefix is the kind label, not part of the pattern).
3. **Critically assess all subagent output first-hand** before absorbing
   (owner standing directive; `verify-dont-trust`).
4. **Comms events are OUT of scope** — owner-routed to a separate session
   under a separate plan.
5. Conserve-by-default (PDR-122, incl. the 2026-07-03 tiered-evidence-reporting
   amendment); the PDR-098 recurrence check applies with the temporal qualifier
   (an instance is fires-despite-home evidence only if it post-dates the home);
   no ledgers — homes and commits are the record.

### Craft the E-b1/ws1g passes proved out (reuse it)

- Judge each item against its home FIRST-HAND: most tier-E kills are
  over-kills of already-graduated doctrine (the no-tools regime failed
  grounding-flavoured tests at 4–7× the free-tool rate — measured,
  `data/banked-verdict-structural-mine-2026-07-03.json`); expect mostly
  verified-duplicates with a minority of genuine enrichments at firing
  surfaces.
- Exclude the corpus data dir from home-verification greps
  (`-g '!**/large-corpus-analysis-tooling/data/**'`) — the checkpoints contain
  the candidate text and self-match.
- Build each commit bundle's file list from `git status`, never from memory of
  the slice's edits.
- Mind `rg -r` (it is --replace, not recursive) — four fires-despite-home
  instances in one session are on the napkin.

### Cadence

One commit per batch; batch boundary = the natural handoff boundary; napkin
capture continuous; buffers empty or explicitly dispositioned at each session
close; closeout reports value and impact, never accounting. zsh does not
word-split unquoted variables (use arrays); never `--no-verify`.
