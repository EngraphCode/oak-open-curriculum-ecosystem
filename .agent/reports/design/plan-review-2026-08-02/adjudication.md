# Design-system-completion plan v1 — tiered review adjudication (2026-08-02)

**Verdict: the plan at `6f3221e1e` FAILS review.** 31 agents (4 haiku landscape → sonnet
first-hand verification → 6 Opus specialists + 1 Fable frame-challenger), 98 surviving or
expert findings, 23 blocking. Finding corpus: [`findings.v1.json`](findings.v1.json)
(run `wf_b02eb59a-e81`, ~2.89M subagent tokens). Adjudicated by the Director (Magnetar
binds Oblivion, 74d914) with the owner's verbatim requirements as the goal axis.

## The convergent failure classes (adjudicated real)

1. **Authoring-time grounding was too shallow — the Director's own defect.** The plan
   asserted repository claims that first-hand reads falsify: "ALL export pages" counted
   as three when the export carries ~80 page artefacts (62 preview pages, templates,
   whitelabel proofs, `ui_kits/oak/index.html`); the hub "gains theme detection where it
   lacks it" when the hub already has pre-paint detection and persisted selection; the
   token-reference page "generated from the DTCG source" when the export's DTCG tree
   cannot support per-identity generation as written and two DTCG trees run in opposite
   generation directions; "themes as data" against five closed hand-maintained theme
   lists in the shipped kit; off-horizontal presented as consistent with the landed
   corpus when in-repo Freedonia is currently the most orthogonal identity (the owner's
   word defines the TARGET — the plan must carry it as a delta, not a description).
2. **Live-plan collision.** W1.5/W2.1 silently duplicate workstreams owned by a
   conserved-but-cited executing plan that ADR-213 §3 names by name; no dependency,
   supersession, or amendment edge. (fred E69, docs-adr E54.)
3. **ADR obligations unnamed.** The plan changes surfaces live ADRs document — the
   showcase reassignment falsifies landed permanent surfaces; W2.1 mandates a component
   shape ADR-213 §3 has a standing rejection of; the identity schema inverts §2's
   source-of-truth direction; consuming `studio-source/` pages triggers §1's
   move-out-under-gate mechanism — with no ADR-amendment story anywhere.
4. **The frame findings (Fable overwatch) cut deepest.** F0: the coverage-matrix logic
   structurally regenerates the rejected showcase's shape (specimen-inventory pages
   bound to no design grammar). F1: the only pre-owner aesthetic instrument is one
   unspecified line. F2: the decomposition is infrastructure-first — the owner's first
   wow verdict arrives after three workstreams of plumbing, inverting what he asked for.
   F5: no workstream allocates design authorship to making EMC² and Freedonia visually
   excellent.
5. **Accessibility blockers.** The hub has no a11y proof surface and no workstream
   creates one; the single-mechanism theme mandate silently drops the OS forced-colors
   route; CSS re-ordering ships with zero reading-order/focus-order criteria; the
   contrast gate does not generalise as written.
6. **Testability.** A large fraction of acceptance criteria are unmeasurable as written
   (test-expert E41–E49; landscape D16–D30 unverified but consistent).

## Routing verdict

The rewrite is **re-routed to the design seat (Corsair hunts Surf, 4d3282)** rather than
cured at the Director seat: the plan's coherence surface (the design estate end-to-end)
must be held by the mind that executes it (PDR-117 lane definition), and the v1 failure
class was precisely authoring at routing altitude without implementer-grade grounding.
The Director adjudicates; the same fleet topology re-reviews v2 (resumable run); the
owner's pass-without-issue gate stands before implementation begins.

Standing owner rulings that v2 must carry in addition to the v1 corpus: iteration is
LOCAL (Claude Design only at owner-instigated moments — no two-way sync investment);
wow-first decomposition (early rendered pages, machinery underneath them); design
authorship for the counter-identities as first-class work.
