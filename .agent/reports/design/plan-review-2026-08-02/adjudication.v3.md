# Design-system-completion plan — round-3 adjudication and LOOP VERDICT (2026-08-03)

**Round verdict: v2.1 does not reach the zero-finding gate.** 31 agents (same
topology, run `wf_121bcbac-abe`): 6 verified landscape survivors, 82 expert
findings, 8 frame findings, 17 landscape rows unverified at the cap. Corpus:
[`findings.v3.json`](findings.v3.json).

**Loop verdict — the material finding of this round: the iteration is
DIVERGING, and a fourth round of the same shape is the wrong move.** The
tally across rounds is 98 → 112 → 113 while the plan grew 655 → 1005 lines.
Round 3's blocking findings cluster on mechanisms the round-2 cures
themselves introduced (the axis token shape, the negative gate cells, the
falsifier constructions, the widened instrument roster, the composition
envelope). The estate's own loop doctrine rules on this pattern: a loop whose
successive rounds do not shrink is a routing failure, not diligence. The
generator is structural: the plan specifies far-horizon stories (W2–W6,
weeks from execution) at implementation-mechanism depth, and every rewrite
adds mechanism mass that draws new mechanism findings. This contradicts the
estate's future-work-items-are-pointers doctrine and burns ~3.3M subagent
tokens per round re-reviewing unbuilt depth.

## What survives adjudication as REAL near-horizon work regardless of loop shape

1. **Faithfulness fixes to the round-2 Director rulings** (react-component
   blocking; design-system E-row; docs-adr blocking): the §3 amendment text
   and W3.0's studio-seed role assertions do not implement ruling (2) as
   given; the axis token shape cannot express the EX28 composition as
   written. Small, unambiguous, Corsair's to cure whenever the text next
   opens.
2. **The instrument self-review defect** (frame, blocking): W0.7's
   design-review instrument is structurally the author judging their own
   work. Cure shape: a non-authoring reviewer leg (any peer seat) plus the
   owner gate that already exists; belongs in W0.7's text.
3. **The counter-identity taste anchor** (frame, blocking): EMC²/PDS wow has
   no calibration data until maximum sunk cost. Cure: an early cheap taste
   probe per counter-identity (a single composed page variant at W1-time),
   priced into W1/W2.9.

## Recommendation to the owner (the gate is his word; the loop-shape change is his call)

Partition the zero-finding gate by horizon:

- **Near horizon (W0 + W1, the wow-first slice): keep full depth and the
  zero-finding bar.** These stories execute now; round-3's near-horizon
  findings (incl. the three classes above) get cured and a SCOPED re-review
  (near-horizon lenses only, a fraction of the fleet cost) runs to zero.
- **Far horizon (W2–W6): demote to pointer-level stories** — goal, owner
  gates, dependencies, acceptance SHAPE — with mechanism depth authored at
  each story's open under the estate's normal pre-execution review
  discipline (per-story expert review, PDR-132 budget). The far-horizon
  mechanism findings in rounds 2–3 are conserved as story-open inputs, not
  plan blockers.

This matches the owner's own wow-first ruling (pixels early), stops the
divergence generator, and converts ~3.3M-token full-fleet rounds into
per-story reviews at execution time. Implementation still starts only at
the owner's word.

Adjudicated at the Director seat (Magnetar binds Oblivion, 74d914),
2026-08-03. Corsair holds (no v2.2 authoring) until the owner's loop-shape
word lands.

## Ratification addendum (2026-08-03, owner card answer)

The partition is RATIFIED — owner's words: "Yes, partition the work, and
make a note that the divergence is teaching us about seams that in future we
could spot in advance", with thanks to the team for spotting it. The seam
lesson, recorded as directed: a plan should specify at implementation depth
only within its execution horizon; beyond it, stories are pointers whose
depth is authored at story open under per-story review. The detector we had
all along: the finding-tally slope across review rounds — flat or growing
tallies against a growing text are the seam speaking. Doctrine-queue
candidate: horizon-seam check at plan authoring time (a plan-body clause or
reviewer lens asking "which stories does this text specify deeper than their
horizon warrants?").
