# Design-system-completion plan v2 — round-2 adjudication (2026-08-02)

**Verdict: the v2 text (reviewed as the working-tree amend, landed in `e3574388b`)
does NOT reach the zero-finding gate. The iteration continues at the design seat.**
31 agents (same topology as round 1, fresh run `wf_368f0694-4a8`, ~3.26M subagent
tokens): 4 verified landscape survivors, 78 expert findings (13 marked blocking by
their finders), 10 frame findings (2 blocking), and 20 landscape rows dropped
unverified at the sonnet cap. Corpus: [`findings.v2.json`](findings.v2.json).
Adjudicated by the Director (Magnetar binds Oblivion, 74d914) with the owner's
verbatim requirements as the goal axis.

## Character shift from round 1 — recorded deliberately

Round 1 failed on falsifiable repository claims (pages counted wrong, behaviour
asserted that existed, generation directions inverted). **Round 2 has none of that
class.** The surviving findings are specification-depth (acceptance criteria that a
seat could satisfy without the intended evidence), doctrine collisions on two of the
four flagged seat-verdicts, and frame-level sequencing critique. This is the
iteration working: the object is structurally sound and the findings are about
making its guarantees mechanical.

## The Director's own doctrine ruling — seat-verdict (a), verified first-hand

Three experts independently converged on seat-verdict (a) (docs-adr-expert
blocking, architecture-expert-fred blocking, react-component-expert blocking):
W3.0's mapping rule as written re-creates the shape ADR-213 §3 rejects. Verified
against the ADR text first-hand at this seat:

- §3 sanctions a **curated** owned tier — "curation on owner substance", named
  first candidates (code chip/block, breadcrumbs, `oak-band`, masthead/footer) —
  and rejects the **wholesale** wrap: "Re-wrapping the class library in a framework
  component layer remains rejected… settled here and… not re-litigated per widget."
- W3.0's rule — "recipe class → component", unconditional, over every census
  class, with the coverage table's mapping column "complete against the census" —
  is the wholesale shape when executed literally.
- v2's own W3 preamble already carries the cure's kernel: "full COVERAGE: every
  W0.1 component class gets a recorded mapping **decision**." A decision vocabulary
  that includes **no-construct / stays-class-only** (with a curation criterion for
  when a class earns a component) dissolves the collision without amending §3.
  The W3.0 rule must say this explicitly; as written it forecloses it.

Ruling: the finding class is REAL and blocking as written; the cure is cheap and
does not touch the §3 rejection. Seat-verdicts (b), (c), (d) each drew expert
findings — they are the executing seat's to disposition per row, with this ruling
as the worked example of the depth expected.

## Routing

The v2.1 iteration belongs to the design seat (Corsair hunts Surf, 4d3282) per
PDR-117 — same discipline as round 1: one recorded disposition per corpus row
(including the 20 unverified dropped-at-cap rows — unverified is not dismissed),
deviations grounded, repository claims verified first-hand before they are cited.
The same fleet topology re-reviews v2.1; the gate remains a zero-finding round,
then the owner's implementation word.

Owner pacing directive (2026-08-02 ~21:23Z) binds the iteration: slow and
deliberate, no parallel bursts; the next fleet round fires when the rewrite is
ready, not before.
