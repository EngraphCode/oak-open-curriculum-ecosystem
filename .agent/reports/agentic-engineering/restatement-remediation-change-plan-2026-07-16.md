# Restatement remediation — change plan of record (2026-07-16)

Owner-directed (this session): no follow-ups — design and guarantee excellence; update the
plan when the plan needs updating; untracked files are FIRST-CLASS (local because
high-traffic and ephemeral, never because they matter less); the freeze may need recutting
after remediation. This record captures the concept exploration and the resulting change
set. The approved plan file (`~/.claude/plans/resilient-wobbling-hartmanis.md`) is updated
in the same pass — this record explains; the plan binds.

## Concept exploration (compressed)

**Observations.** (1) The T3 audit corpus was defined by tracked globs, structurally
excluding the untracked live coordination tier — exactly where the invented-gate sweep's
two INVENTED exemplars lived (Director handoff records) and where today's three
staled-within-hours instruments live. (2) My own self-review deferred its cures to "a
follow-up sweep" and "the next consolidation" — deferral vocabulary reproducing the
excellence leak. (3) The S0 frozen denominator ages as remediation edits land on live
plan-estate files; divergence accumulates in classes the merge-recheck exists to judge.

**The reframed question.** Not "which files are tracked?" but: which surfaces are
AUTHORED-LIVE (bound by the single-source contract and in audit scope, tracked or not),
which are DERIVED (generated + validated, never hand-edited), and which are HISTORICAL
(conserved, history-guard exempt)? Every design decision below keys on that axis.

## The change set (all planned now; no follow-ups)

### C1 — Audit corpus amendment: T3 → T3+U (the untracked live tier joins)

New corpus group (h), authored-live untracked surfaces:
`.agent/state/collaboration/handoffs/*.md` (47 files),
`.agent/collaboration/rapid-comms/*.md` (ARC channels),
`.agent/reports/agentic-engineering/*-2026-07-1[56].md` (live arc reports),
plus `.agent/reports/restatement-audit/{gazetteer,canary-key}.v1.json` classified
DERIVED-class (finders treat as `generated` assertionKind sources, not targets).
History guard unchanged: records' past-tense sections classify REAL-as-history.
Fleet arithmetic: ≈ +55 files → +8 map windows, +20–40 voters.
**Amended P12 ceiling: 480 invocations / 6M tokens** (from 400/5M) — authorized by the
owner's untracked-first-class directive in-session this hour; the price point matches the
already-approved r2 pilot class. Consumers updated in this pass: the audit brief (§Job 2
corpus + ceiling), a directed corpus-amendment event to Vole (their Job 2 has not
dispatched; zero disruption), and the plan file Deliverable 2.

### C2 — The continuity-instrument contract lands NOW (tracked doctrine + live briefs)

The five-point contract from the self-review (pointers for anything with a live home;
inline values only load-bearing and marked "at freeze"; judgment in full; precedence +
supersedes declarations; conservation index) is not consolidation cargo — it amends the
session-handoff SKILL and the handoff-messages-self-contained rule via a small docs PR in
this arc (Director-authored), and is applied DIRECTLY to the live untracked briefs
(dispatcher + audit) as edits in this pass: each gains a PRECEDENCE block ("this brief
supersedes the following cited-document statements: …; on any other conflict, flag to the
Director, do not choose") and at-authoring markers on volatile values.

### C3 — Instrument remediation dispositions

The compaction record: §2 collapses to a pointer at the plan file; §1's cached
auto-merge/PR states gain "at freeze" markers (already conditional in part). The
Mussel→Tuna record and older handoff records: HISTORICAL — conserved untouched (their
lessons live in the reviews and the fleet ledger will cite them as history, not cure
them).

### C4 — Freeze recut: a criteria-gated plan step, not a possibility

After the fix-ledger cures land (plan Deliverable 3): run `refound-verify-freeze` +
`refound-merge-recheck` over the accumulated live-vs-frozen deltas. RECUT CRITERIA — cut
frozen-v2 (the freeze rule's landed v2 arm) when EITHER (a) unsanctioned deltas touch
denominator files, or (b) the remediation cures materially alter content whose frozen-v1
copies feed active proofs (tiling / expected-reach / census). A recut re-derives the
denominator, re-runs tiling to exact-cover green, and re-anchors Walk A's inputs on the
v2 cut; the recut itself is an owner cutover sanction (register row to be cited at the
moment). If neither criterion fires, the v1 cut stands and the merge-recheck
classification is the recorded evidence.

### C5 — Continuity-surface remediation joins the plan (from the continuity review)

The extract/split/generate verdicts become plan work items, not recommendations:
director-handoff CURRENT-HANDOFF-STATE extraction + ledgered cures (Director, rides the
cures PR); repo-continuity split with the `continuity render` generated-state projection
(tooling item, specced from fleet-pattern capture); declared-cache banners; thread-record
WIP-truth relabelling (Director); napkin + frictions-register graduation via the
owner-spawned curator session (its brief will follow the C2 contract).

## Ratification state

Corpus + ceiling amendment (C1): owner-directed in-session, this hour. C2/C3/C5:
Director-authority doctrine and instrument work under the approved plan's scope. C4: the
criteria are planned now; the recut MOMENT carries its own owner sanction per the gate
register. The plan file is updated in the same pass as this record.

— Mussel rides Coral (6f8857), sitting Director, team Mango
