# Plan-estate refounding — design-panel record (2026-07-06)

**What this is.** The durable record of the design exploration that produced
[`plan-corpus-refounding.plan.md`](../../plans-backlog-2026-07/product-development-governance/active/plan-corpus-refounding.plan.md):
a six-facet independent design panel plus a four-lens adversarial critic pass (PDR-123
shape; 10 agents, ~1.28M subagent tokens, zero errors), run against an owner commission
and the Practice donor estate's live exchange input. Working files were ephemeral
(session-local); everything load-bearing is conserved here and in the plan — per the
important-state-not-in-temp-files rule.

**Cross-estate provenance.** The donor estate's exchange seat (session prefix `2a5066`;
this estate's derivation "Kiln tracks Basalt") delivered a pin-free process synthesis +
ten-item hyper-efficiency kit to the incoming box
([`resonance-plan-estate-refounding-synthesis-2026-07-06.md`](../../practice-core/incoming/resonance-plan-estate-refounding-synthesis-2026-07-06.md)),
answered five ground-truth questions (retrospective delta at ~40× scale; the
residual-judgement map; verification economics; typing a messy lifecycle; a do-not-copy
list), and proposed twelve draft invariants — all conserved in the exchange ARC channel
record. The donor's own doctrine substance re-sources from
[`resonance-practice-knowledge.md`](../../reference/resonance-practice-knowledge.md).

**Commission.** Owner, in-session, 2026-07-06: a super-efficient, provably complete and
lossless protocol for refounding this estate's planning corpus around intent, in
appropriately sized batches with error correction and loss checks, using hyper-efficient
low-context subagents that make no judgements ("if they make judgements we lose
information"); documentation updated; one cohesive long-term plan from the current estate
through the improved planning corpus to the eventual ADR-200 intent graph.

## 1. The estate facts the design rests on (measured this session)

- `.agent/plans/**`: 618 markdown files / 165,066 lines (335 `*.plan.md`, 17 census areas
  — the F1 designer counted 629 md + 38 non-md with milestones/proposals included, and 22
  top-level directories; the deterministic denominator script settles the exact figure at
  freeze time, which is the point of having one).
- Sweep surfaces (not freeze sources): `.agent/plans-old-archive/**` (571 md, ~230k
  lines), `.agent/prompts/`, thread records — together ~59% of in-scope text.
- Known defects: ~30 emergent free-text `status:` values across two parallel
  vocabularies; ungoverned `serves_stream`; orphan `serves_thread` references; 149/355
  docs unlinked; 59 stale executables; no plan-state recomputation tool.
- Donor sizing prior: 15 sources / 4,452 lines / 1,443 inventory lines / 223 ledger rows
  / ~17h elapsed including owner gates (an upper bound — it contains an overnight
  owner-hold); adversarial passes produced essentially all new semantic truth. Of the
  donor's ~1.3M-token verification-wave spend, the MECHANICAL waves (~750k) are deleted
  here by scripting the mechanical layers (their own participants' recorded cure); the
  adversarial-pass class (~550k there) is precisely this protocol's challenge layer —
  kept, and priced as the dominant term.

## 2. The six facet designs (summaries; full decisions absorbed into the plan)

- **F1 — mechanical substrate.** A checked-in, owner-ratified surface-class freeze rule
  (never a per-file filter); one atomic S0 commit landing verbatim frozen copies + a
  sha256 line-level denominator + identity proofs; a standing re-hash gate in
  `repo-validators:check`; denominator re-derivation after every merge with a versioned
  append-only amendment mechanism; three overlapping deterministic regex nets emitting a
  verbatim, digest-carrying `inventory.jsonl`; residue clustering with declared orphan
  bounds and a scripted planted-synthetic-orphan proof; granularity-agnostic exact-cover
  tiling, mutation-tested; ~10 scripts homing in `agent-tools/src/refounding/`; global
  stable points S0/S1/S2 then per-area batches; zero LLM tokens at run time.
- **F2 — worker layer.** Thin by design: exactly two roles. `refound-reader` (the second
  blind net: Read-only, bounded turns, verbatim quotes + anchors only) and
  `refound-locator` (single-target, zero-tools, dispatcher-inlined windows). Over-inclusion
  doctrine (workers only ADD candidates — never subtract, filter, rank, classify);
  schemas with no free-text; 100% four-step verification with full-set byte equality and
  hidden known-answer floors in every dispatch; refusal (JUDGEMENT-REQUIRED) terminates
  the task and blocklists its spec hash — redesign mints a new hash, so re-wording a
  refused task at a worker is structurally impossible; a typed dispatch ledger makes
  batch completion arithmetic.
- **F3 — judgement and error correction.** Judgement placed, never eliminated: a
  placed-judgement map with everything below the line forbidden to carry judgement; a
  closed conserving disposition taxonomy with NO discard class (conserve-by-default as a
  type-system absence); two-lens quorums honouring the measured n_eff ≈ 1.4 (lens count
  is the wrong lever); mechanical probes decide re-expression-ending classes first, then
  cross-regime concurrence, else conserve-to-holding flagged terminal-candidate;
  disposition-class-stratified challenge (as designed: ALL loss-bearing rows challenged,
  probe classes mechanically re-verified — one point superseded by the donor-seat
  correction, §3 row 22: coverage is universal, stratification narrows the question);
  ~15 sealed known-answer canaries + three planted defects behind a deterministic
  breaker; an eight-item scripted batch-close loss check; a five-level error-correction
  ladder ending in pre-declared halts.
- **F4 — intent layer and lanes.** The intent layer is the standing owner-ratified
  wording quoted verbatim (the mission; "deliver Oak's rigour at reach and at pace"; the
  three streams; the signed-off APP/TOOLS/FRAME choice registry) — never re-authored.
  Lanes are registered axis values in a machine-validated lane registry forming a
  partition, each anchored to a strategic choice, a named capability, or the single
  conserving holding lane; five validator rules, each prove-it-fires. Holding-lane
  content is conserved as `kind: strategic` plans with promotion triggers (never an open
  paused state); >20% of the corpus held falsifies the taxonomy. Newly-identified lanes
  exit only via an additive choice/capability proposal or holding. Coexistence is
  governed by sanctioned write classes + an accretion log; two scripted additive banner
  classes; live-lane coordination uses the existing claims/comms/commit-queue substrate
  with defer-don't-contest.
- **F5 — recomputable state.** Kit item 9 affirmed with a correction: build the
  plan-state recomputation tool FIRST as one deterministic engine behind two adapters — a
  permanent gate adapter over V0.1 proof-typed frontmatter, and a disposable audit
  adapter feeding the same engine from the frozen inventory via a versioned
  status-mapping table — so the two-verdict audit is a zero-token recomputation and the
  refounded corpus is born gated. Proof-typed todos land as an additive V0.1 extension
  (owner re-ratification: V0 todos are LOCKED). Six-kind proof taxonomy mapped to this
  estate's recomputers (gate registry, probe templates with closed survivor sets, git/gh
  facts, ratified records); unmappable statuses are a named UNMAPPED residue class;
  >20% UNMAPPED halts for a table v2; attested-count is a reported signal, never a gate;
  the exact team-state join defers to the composed-liveness lane via an additive claims
  schema pointer.
- **F6 — sequencing and roadmap.** Three eras (current estate → refounded corpus →
  intent graph); the refounded-corpus milestone is a committed stable point with
  standalone acceptance, valuable even if the graph era were never to run. The
  refounding inserts under ADR-200 with WS6's harvest substrate becoming the refounded
  corpus and the no-loss audit composing transitively (old→refounded ∘ refounded→graph);
  WS2/WS4 never wait (write-surface disjointness, additive authoring, schema pinning,
  Director-scheduled gates). Borrow corpus-generalisation doctrine, priors, and tested
  deterministic modules; never wait for its landing set — and this arc produces its P2
  substrate. Pilot = the whole `discovery` area (16 md / 9 plans / 3,537 lines; no
  active lanes; exercises the holding lane; fallback `user-experience`); one whole-estate
  freeze; batches ascend by liveness with `product-development-governance` last; no
  long-lived working branch — per-batch worktrees landing stable points to `main` via PR.

## 3. The four critiques and every critical finding's disposition

Verdicts: lossless-adversarial **needs-rework** (7 majors, 5 minors);
assumptions-proportionality **sound-with-revisions** (4/6); economics
**sound-with-revisions** (5/4); oak-doctrine-fit **sound-with-revisions** (2/6). The
convergent judgement: individually sound mechanisms, with the defects concentrated in
cross-facet connective tissue — the predicted failure class. Every critical finding and
its disposition in the synthesised plan:

| # | Finding (lens) | Disposition in the plan |
| --- | --- | --- |
| 1 | Ledger-row production owned by no facet (all four lenses) | Default rows = F1's deterministic anchored blocks; ONE unified F3-owned row-judgement stage (J4) emits segmentation-delta + disposition + home + lane per row, lane evidence as input column, with the home-lane reconciliation rule |
| 2 | Phantom "mechanical pre-partition" cited circularly (assumptions, economics) | Deleted as a phantom; cost re-declared at full volume (~8.3k row prior from the donor's measured 14.9 rows/source); any real pre-partition would be an F1 rule ratified at G1 |
| 3 | Full-estate lane-evidence quorum (~5–8M tokens) before any calibration (assumptions, doctrine, economics) | Pilot-first evidence pass only (stratified sample + sealed lane canaries); Walk A ratifies with non-pilot lanes at candidate status; remainder runs per-batch after G-SP3 |
| 4 | Sequencing inversions: SP1 needs post-freeze inventory; Walk A listed before its inputs; freeze held hostage to tool build (assumptions, lossless) | R0b acceptance = fixture + mutation-proof only; freeze+inventory land as soon as their own proofs pass; R1 order fixed: freeze → inventory → audit-mode run → (pilot) evidence → Walk A → pilot |
| 5 | Owner-gate union never aggregated (~15–20 sittings, duplicates, missing cutover sanction) (assumptions, economics) | The consolidated owner-gate register (R0c) — G0..Walk C, duplicates merged, cutover sanctions ride ruling batches, escalation-thinning tables ratified at Walk A |
| 6 | The re-sequencing is a Decision-level ADR-200 change recorded as a sequence insert — F6's own falsifier fires (doctrine) | G-ADR: explicit owner re-ratification of the Q3 boundary, the audit-composition ruling, and the V0.1 sign-off — a named gate before S0, not a silent insert |
| 7 | Lane validator narrows the V0 LOCKED `pending` sentinel at the wrong gate (doctrine) | The pending/lane interaction is bundled into G-ADR's V0.1 packet (owner re-ratification path), not a lane-validator side effect |
| 8 | Closed disposition taxonomy lacks the permanent-home route ADR-200 requires (doctrine) | Seventh conserving class `permanent-home-routed` (destination + frozen binding clause + destination review), challenge-covered as loss-bearing (P14, P7) |
| 9 | Artefact homes contradict; some outputs nest inside the frozen denominator (doctrine, lossless) | One canonical map: run artefacts at a refounding root OUTSIDE the denominator; mapping table in agent-tools; sanctioned-writer classes in the freeze rule (P2) |
| 10 | New multi-writer artefacts lack PDR-049 merge classes (doctrine) | Every new shared artefact declares a merge class at creation; single-writer-per-file shapes preferred (Foundation alignment §PDR-049) |
| 11 | Coexistence-window deltas destroyed at retirement while proofs read green (lossless) | frozen-v2 versioned arrival copies with scoped inventory/tiling extension; banner-aware byte comparison with its own mutation proof; the scripted retirement precondition (live bytes == frozen + banner + routed amendments) |
| 12 | ~242k sweep-surface lines behind a single never-proven-fireable net (lossless) | Marker-free work-bearing paraphrase plant required caught (P4); declared-rate reader sample of NON-hit sweep windows as second net; otherwise an explicit G1 residue declaration the owner signs knowingly |
| 13 | Probe-decided block interiors: no per-line detector; reader findings routed to lone adjudication (lossless) | Regex-invisible calibration canaries by construction; a verified reader finding inside a probe-decided block voids terminal status and routes to challenge/cross-regime; batch close requires no un-dispositioned reader finding inside terminal spans (P7) |
| 14 | Destination-corpus rooting collides with the freeze denominator (lossless) | Rooting decision forced to G1 as a named freeze-rule class consumed by merge-recheck; paired red gate on non-conforming content under the new root |
| 15 | Cross-batch home mutation un-verifies passed challenges silently (lossless) | The challenge-stale mechanical trigger: ledger home ids joined against git diffs; stale rows block affected retirement tranches and surface at Walk C (P13) |
| 16 | "Audit as recomputation" inflated: no claim census or probe-proposal stage designed (lossless) | The claim-census script (R0a) + probe-proposal as placed judgement (J5) + locator anchors before any terminal disposition (P5) |
| 17 | Reader priced below its own measured prior; no cache assumption (economics) | Declared at the measured ~48k/window prior with the uncached figure as ceiling |
| 18 | Duplicated census-scale lane judgement (F4's pass vs F3's lens) + contradictory lens counts (economics) | Folded: lane evidence is an input column of the ONE row-judgement stage; 2-lens + escalation is the single lens doctrine |
| 19 | No owned cost roll-up; headline below the physical reading floor; wall-clock long poles unpriced (economics) | R0c's Director-owned cost ledger; corrected declaration 40–60M tokens / 25–45 seat-sessions; Author + Adjudicator throughput lines; arrival-halt loop priced per week; fixed-vs-marginal split re-priced at SP3 |
| 20 | Owner ruling queue saturates (demand 100–200 vs ~45–90 slots) (economics) | Escalation-thinning policy tables ratified at Walk A; demand re-sized from pilot actuals at SP3 (P11) |
| 21 | Unfalsifiable archive-reader spend outside the denominator (economics) | Deleted; the archive is scripted-sweep-only; promoted hits enter reader scope via normal amendment mechanics, paid once |
| 22 | Donor-seat self-correction (2026-07-06 20:29, ARC channel): the class-stratified challenge exempted reason-bearing classes — wrong-reason risk is total loss no probe can see; the donor record challenged ALL classes | P7 rewritten: challenge EVERY class; stratify the challenge QUESTION per class (loss-bearing → detail-reach; already-complete → proof-recomputes; superseded-because → attack the reason; owner-rejected → ruling-record coverage); probes pre-verify, never substitute. Plus epistemic labels absorbed: challenge cost ~linear in corpus size (lever = brief narrowness); the donor ~17h is elapsed-time upper bound, not a work estimate; any pre-partition rule needs its own planted-defect proof |

### 3a. The cross-estate adversarial review (the donor seat, 2026-07-06 21:05)

The donor estate's exchange seat reviewed the full synthesis first-hand (both artefacts
end-to-end; measured claims mechanically recomputed — including catching the live
denominator at 619 md / 165,468 lines, +402 lines from this session's own
protocol-authored files: a live worked proof of P2's sanctioned-writer-classes clause).
**Verdict: sound-with-revisions; zero overturns on the seven attacked synthesis
decisions**; several mechanisms (the challenge-stale trigger, the spec-hash refusal
blocklist, the no-discard-by-construction taxonomy, the audit adapter) flagged for
backflow consideration to the donor estate. Findings and dispositions, all absorbed
pre-commit:

| # | Finding | Disposition |
| --- | --- | --- |
| B1 | The challenge layer had no planted-loss canaries — a zero-findings pass on a well-authored batch is indistinguishable from a blind one | P4 extended: sealed known-dropped-spec-detail plants at a declared rate in EVERY batch's challenge stream; a challenge pass is accepted only if its plants were caught; R0a plant tooling + r3/r4 acceptance updated |
| B2 | The WS6 transitivity claim needed its denominator stated at decision level (block/row-granular conservation vs idea-granular harvest) | The harvest substrate is stated explicitly — refounded corpus PLUS frozen archive via binding-clause provenance edges — in r6's hand-off note, the G-ADR ruling content, and the rewrite-plan amendment |
| M1 | Segmentation-delta votes are structural and do not aggregate deterministically | J4 aggregation rule: identical deltas = agreement; any structural disagreement routes to J3; pilot measures the disagreement rate (high rate = F1 granularity fix) |
| M2 | Candidate-lane promotion mechanics and the taxonomy-refit trigger were unnamed | r2 acceptance: the register names promotion evidence (via ruling batches) and the per-batch refit trigger (holding-share trend + lane-churn rate) |
| M3 | `permanent-home-routed` risked a bespoke graduation path (PDR-122 Consequences forbids) | P14: the class routes candidates+provenance INTO the existing consolidation machinery; curation-load spikes are Director-scheduled |
| M4 | Challenger-finding adjudication unpriced (the donor's hidden dominant seat-cost: ~26 findings/100 rows) | A first-class priced ledger line + pilot metric (findings per 100 rows, by class) |
| M5 | Canary sealing mechanics unstated (in-repo keys contaminate a Read-capable fleet) | Hash-commit-then-reveal (or dispatcher-held keys) named in P4/R0a |
| M6 | The no-active-lanes pilot under-exercises the three first-break terms (arrivals, challenge-stale, ruling demand) | SP3 labels evidence-vs-prior lines; batch 2 is chosen WITH active lanes and re-confirms those terms before full parallelism |
| m1 | "Irreversible" is attention-irreversible (content recoverable) | Named in J4; pilot reports terminal-class overturn rate separately |
| m2 | "Verification-wave class deleted" overclaimed | §1 corrected: mechanical waves deleted; the adversarial class is the challenge layer, kept and dominant |
| m3 | Walk C's composed chain rested on one fresh-context reviewer | Two fresh-context reviewers on the composed chain (r6) |
| m4 | The reader's main-corpus role could be mispriced as universal | Stated: targeted second net only; the universal semantic pass is the challenge layer |

Major (non-critical) findings from the panel critics and their homes: the B2 banner class generates only from
conservation-ledger rows (no third disposition vocabulary); mapping-table versions are
data revisions of one table (no v1/v2 parallel-file family, honouring replace-dont-bridge);
invented thresholds (overturn budget, canary mismatch budget) are declared starting
values ratified at the tables gate and re-examined at SP3 — owner-visible, not
owner-invented; mass-bannering lands as one sanctioned diff class within a declared
commit window.

## 4. Resonance mechanism integration table (adopt / adapt / reject, with reasons)

| Mechanism (donor) | Verdict | Reason |
| --- | --- | --- |
| Freeze-first, mechanical source rule, stable denominator | **Adopt** | The conservation chain's root; judgement placed once at rule authoring (G1) |
| Byte-identity + line-level denominator + tiling ledger + planted-orphan proof | **Adopt (scripted)** | Deterministic at any scale; every detector proves it can fire |
| Worker-fan-out extraction | **Reject** | The donor's own recorded cure: their context-economics artefact; scripts produce the same inventory exactly (their dispatcher recomputation was always the binding proof) |
| Two-verdict state audit before authoring | **Adopt (mechanised)** | Via the recomputation tool's audit adapter + claim census + placed probe-proposal — recomputation, not re-reading |
| Challenge-the-clean adversarial pass | **Adopt (question-stratified, every class)** | ALL rows of EVERY class challenged (donor correction: their own record challenged all 223 rows; wrong-reason risk on reason-bearing classes is total loss); stratification narrows the QUESTION per class; probes pre-verify claim-bearing rows |
| Frozen-spec binding clause | **Adopt (from first draft)** | The donor's post-challenge rework cure, applied at authoring; narrows challenge briefs |
| Repoint-before-retire in one commit | **Adopt (per tranche + scripted precondition)** | Gates only ever see finished states; the precondition script closes the coexistence-delta hole |
| Additive dated supersession notes | **Adopt** | The history-vs-usefulness third way; no-tombstones compatible |
| Line-number citations into live docs | **Reject** | Rotted within hours at the donor; stable ids from first draft |
| "Returns nothing" acceptance probes | **Reject** | Closed sanctioned-survivor sets instead (donor kit item 4) |
| One-session ambition | **Reject** | Multi-session by design; committed stable points; proof-not-clock exits |
| Plan-state recomputation tool built after the arc | **Invert** | Built FIRST (kit item 9): the estate is born gated instead of born audited |
| PDR-125 verification protocol (four-step, 100%) | **Adopt (hardened)** | Full-set byte equality (never sampling — their measured misses), hidden known-answer floors, spec-hash blocklist on refusals |
| Six-kind proof taxonomy (PDR-129) | **Adopt (registry-backed)** | Gate/probe kinds resolve against committed registries, never free-text shell |
| Recomputable team state (PDR-130) | **Defer (partial)** | Warn-only heuristics now; the exact claims⋈todos⋈git join waits on the composed-liveness decision, via an additive claims-schema pointer |
| Donor-naming neutrality (their PDR-127) | **Honour outbound** | Their constraint is asymmetric; this estate names them freely at home but sends donor-neutral content into their estate |

## 5. Owner-gate register derivation and open questions

The consolidated register lives in the plan (§Owner-gate register). Derivation: the four
facets' gate lists were merged (duplicates: the status-mapping table appeared twice; the
commission's content contradiction resolved by settling content at G1), the missing
per-tranche cutover sanction added, and technical constants downgraded from
owner-ratified to owner-visible declared values.

Residual open questions routed forward (none blocks R0): TOOLS lane granularity and the
cross-cutting quality-surface lane (Walk A decides from evidence); the Oak Innovation Kit
lane vs the open fourth-stream decision; milestones/proposals folding (follows the G1
surface verdict); `serves_stream` retirement after a consumer probe; regime-B definition
(model tier vs envelope — decided at the tables gate with kill-direction calibration);
`disposition: done` plan-level acceptance beyond all-todos-green (V0.2); WS3's re-scope
over the refounded corpus (confirm-at-the-workstream); the P0-restart scheduling
relative to the agentic-engineering-enhancements batches (owner-attention scheduling
only); whether the 149 unlinked docs become a ledger disposition input (F3 at R1).

## 6. Economics summary (pre-pilot; the ledger is R0c's deliverable)

Corrected ensemble: **40–60M tokens** (the 20M headline floor was below the 5–7
corpus-reading-pass physical minimum), **25–45 seat-sessions**, calendar conditional on
concurrent multi-seat operation. Dominant terms: the challenge layer, the unified
row-judgement stage, and Author/Adjudicator wall-clock. Deleted spend classes: the
donor's verification waves (scripted), the archive reader (sweep-only), the duplicated
lane pass (folded), the default third lens (~0.4 effective votes for a full lens price).
First breaks at scale, in order: owner ruling queue → adjudicator throughput → the
arrival-halt feedback loop; each carries a named cure in the plan.

## 7. What happens next

The plan is queued in `current/` pending the owner gates G1 + G-ADR (one sitting): the
freeze-rule packet, the Decision-level ADR-200 amendment, and the V0.1 sign-off. The
donor estate's exchange seat reviews this synthesis adversarially before the owner walk.
WS2/WS4 of ADR-200 proceed independently throughout. The refounding's first mechanical
landings (R0a scripts with their discrimination proofs) can start on owner go.
