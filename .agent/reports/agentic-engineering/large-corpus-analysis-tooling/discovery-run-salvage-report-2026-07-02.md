# Discovery-Run Salvage Report — 2026-07-02

The salvage ws1 deliverable: the full stratification of the 2026-07-02 discovery run's 246
candidates into evidence tiers, computed deterministically from the committed checkpoints and
the banked free-tool verdict corpus with **zero validate re-spend**. Generated by
`pnpm salvage-driver` (agent-tools) over
[`data/discovery-run-salvage-tiers-2026-07-02.json`](data/discovery-run-salvage-tiers-2026-07-02.json)
(the machine-readable table this report renders) alongside the repaired post-run close
[`data/discovery-run-post-run-close-2026-07-02.json`](data/discovery-run-post-run-close-2026-07-02.json).

## Context

The run's map and reduce stages ran sound (580 leaves, 246 genuine candidates). The validate
stage's judgment regime failed calibration: it killed 11 of 18 known-real baselines the run had
correctly found, so the 26-survivor list is not a complete discovery set (Choice-B MISS, strict
within-remit 0.2). The owner directed salvage-not-rerun. Every number below is recomputed by
code from recorded judgments — the frozen adjudication math replayed, never re-derived, and no
LLM in the loop (PDR-122).

Two repairs unlocked the salvage:

1. **The corroboration cwd bug is fixed.** The post-run driver's on-disk home check resolved
   repo-relative claims against the agent-tools working directory, reporting 0/18 corroborated.
   Anchored at the repo root, the truth is **18/18 corroborated, 0 missing claims** — every
   home the meta stage named exists.
2. **The banked Opus quorums are replayed through the frozen `finaliseQuorum`.** 48 candidates
   carry lensed Opus verdicts; 47 form complete three-lens ensembles (C54 is the sole
   incomplete); **22 quorum keeps**.

## Headline partition (conservation-checked: 246 of 246)

| Stratum | Count | Meaning |
| --- | --- | --- |
| Tier A | 18 | Sonnet keeps that are disk-corroborated or Opus-quorum-kept — highest confidence |
| Tier B | 8 | Remaining Sonnet keeps — survived the harshest filter, uncorroborated |
| Tier C | 18 | Opus-quorum-keep / Sonnet-kill disagreements — the over-kill signal |
| Tier D | 18 | Kills the recall judgments name as baseline-matching — proven-real false kills |
| Tier C∩D | 3 | C03, C04, C07 — counted once in the partition |
| Tier E | 187 | Remaining kills, ranked by recomputed evidence for the owner's manual round |
| Residual | 0 | No reroutes, holds, or undisposed candidates |

The recomputed tier C equals the salvage plan's named 18 exactly (C03–C05, C07, C10, C11, C15,
C18–C21, C25, C33, C35, C37, C45, C47, C52) — independent confirmation from the banked
checkpoint, not inheritance. C36 remains the lone reverse disagreement (Sonnet kept it; the
Opus quorum killed it); it sits in tier B on the Sonnet keep.

## Novelty stratification of the 26 survivors

From the repaired post-run close (triage bands recomputed over the corrected corroboration):
12 moderate / 14 review-first; **8 survivors are genuinely novel** (no corroborating home on
disk — new discoveries, not re-finds), split 4 moderate / 4 review-first. The 18 corroborated
survivors are re-finds of knowledge that has already graduated — they validate the pipeline's
recall rather than adding new content.

## Tier A — highest confidence (18)

All 18 are disk-corroborated; four (C02, C38, C39, C42) are additionally Opus-quorum keeps.
Windows/leaves are recomputed spans, never self-reported counts.

- **C01** (9w/9L, recurrence) — a bare `git commit` or an all-files staging sweep on a shared
  index sweeps a peer's staged or uncommitted files into the wrong commit; cured by explicit
  pathspec commits.
- **C02** (9w/9L, recurrence, quorum) — the whole-tree (not staged-only) pre-commit gate chain
  blocks one agent's clean commit on an unrelated peer's in-flight drift.
- **C06** (4w/6L, recurrence) — stash/checkout/reset used as quick cleanup destroys
  uncommitted, fsck-unrecoverable work; forward-removal (Edit/Write) is the only sanctioned
  undo.
- **C38** (3w/3L, recurrence, quorum) — E2E tests fail intermittently under parallel gate load
  from shared state destroying isolation; the cure is deletion or fix, not retries.
- **C39** (2w/4L, recurrence, quorum) — a test-convenience override added to ease an
  integration test becomes a bypass of the very gate being tested, or leaks disk env state
  into tests.
- **C42** (2w/3L, recurrence, quorum) — a green gate is not evidence it works: the load-bearing
  acceptance is that the gate still FAILS on a known-bad input (RED proof).
- **C63** (4w/6L, recurrence) — a piped or backgrounded command reports the trailing stage's
  exit code, masking a red gate as green; capture the real command's status immediately.
- **C65** (2w/2L, recurrence) — the aggregate gate's opening clean step deletes shared build
  output from under concurrent peers, killing their CLIs and watchers for the duration.
- **C88** (2w/2L, recurrence) — numbered claims about filesystem-discoverable populations drift
  silently out of sync with the actual filesystem.
- **C89** (4w/5L, recurrence) — sub-agent reviewers produce false positives on existence checks
  and fabricate quotes; their factual claims must be verified first-hand before acting.
- **C98** (4w/4L, recurrence) — reviewer and bot verdicts are input-to-verify, not binding
  instruction; absorbing every finding inflates plans and re-litigates decided direction.
- **C102** (3w/4L, trajectory) — owner-authorised verify-skipping commits are still blocked by
  the repo's PreToolUse hook as a second safety layer; equivalent skips bind to the act, not
  the syntax.
- **C103** (4w/4L, recurrence) — hook-policy substring matchers fire on forbidden bigrams
  inside prose that warns against them; pathogen-defining docs need self-exclusion.
- **C108** (11w/11L, trajectory) — fitness pressure is an advisory routing signal, never a
  licence to trim knowledge; hardened into the absolute knowledge-preservation rule.
- **C118** (2w/2L, recurrence) — tombstones naming-and-negating a removed idea are reflexively
  re-inserted even during explicit tombstone-removal work, because they read as precision.
- **C134** (2w/2L, recurrence) — the all-channels comms watcher must be armed via the Monitor
  tool for event-driven wake, not a polled background task; a constitutive precondition.
- **C146** (5w/6L, recurrence) — state assertions must be re-derived at compose time of the
  event, never carried from a prior closeout's prose or a stale read.
- **C185** (2w/2L, recurrence) — the enforce edge of capture→distil→graduate→enforce is the
  open loop: graduated lessons land as passive prose rather than enforcing rules or hooks.

## Tier B — uncorroborated survivors (8)

Survived the harshest filter with no on-disk home — these are the genuinely novel keeps plus
near-novel process findings: C36 (the supertest-vs-real-transport E2E classification
trajectory — also the lone reverse, Opus quorum would kill it), C46 (markdownlint misfires on
wrapped-line prose connectors and its autofix fragments prose), C54 (the max-lines rule must be
satisfied by responsibility splits, never TSDoc compression — also the sole incomplete Opus
quorum), C64 (a green aggregate gate certifies only the suites it runs; filtered type-checks
resolve stale dist types), C116 (the single-session-baton memory plugin failed under
parallel-session reality and was replaced by versioned multi-slot surfaces), C123 (memory/state
files are reconciled semantically as concept-unions, never git line-merged), C129 (coordination
CLIs resolve state cwd-relative and silently misroute from worktrees), C135 (the comms Monitor
watcher's four distinct failure modes).

## Tier C — the over-kill signal (18)

Complete Opus ensembles voted keep on all 18 by strict majority while the Sonnet no-tools
regime killed them. Three (C03, C04, C07) are also tier D. Highlights beyond the C∩D three:
C05 (commit-coordination escalated from bare claims to an ordered queue with exact staged
verification), C19 (type-shortcut casts replaced with guards and indexed-access on generated
unions), C21 (fixes to generated code always land at the generator, never the output), C25 (a
dead never-populated ELSER field silently degraded hybrid search to BM25-only for months), C33
(a test file's suffix is load-bearing, gating inclusion and exemptions), C35 (a targeted test
filter silently expands to the full run and reports green with zero tests matched), C45 (the
serial gate chain hides downstream failures behind upstream ones), C47 (markdownlint without
the dot flag matches zero files under dot-directories and exits green), C52 (an in-gate
autofixer silently reverts an authored edit, so a declared fix never persisted).

## Tier D — proven-real false kills (18, all note-named)

Every entry is a kill the meta stage's recall notes name as carrying a baseline's substance
(no `matchedCandidateId` pointed at a kill on this run — all four matched ids are keeps).
Grouped by naming baseline:

- **capture-does-not-cure** — C183 (owner metacognition intervention is the only mechanism
  that surfaces deep misframes; the loop is owner-mediated, so the cure must be a mechanical
  action-time interrupt), C184 (passive doctrine loses to artefact-gravity at the moment of
  action; naming a failure mode does not inoculate against it).
- **identity-tuple-insufficiency** — C139 (the tempfile-collision incident drove identity to
  name-plus-UUID with timestamped frontmatter), C140 (coordination surfaces keying on display
  name corrupt under rename or same-name sessions).
- **commit-editmsg-single-writer** — C07 (also tier C), C149 (agents independently converge on
  the same emergent workarounds — a signal for native CLI support).
- **compaction-is-a-checkpoint** — C137 (a detached heartbeat loop survived 21 hours across
  compactions and every task stop; process trees outlive their monitors), C214 (context-loss
  probes are first-hand-only and must fire at the compaction boundary).
- **cron-template-overrides-owner-direction** — C136 (free-form cron bodies go silently stale;
  the cure is state-bound bodies derived from the claims registry; also names C137).
- **skill-invocation-not-owner-direction** — C111 (a skill step can mandate exactly the
  artefact a standing owner-principle forbids; the principle wins and the skill is the defect),
  C205 (an owner's tentative hedge is repeatedly misread as imperative execution
  authorisation).
- **reviewer-pre-execution-catch** — C95 (pre-implementation adversarial review is the
  highest-leverage practice, catching defects static gates cannot).
- **claims-open-minimum-field-silent-reject** — C128 (repeated CLI flags with last-write-wins
  semantics and undocumented required flags force raw invocations to see real diagnostics).
- **presence-vs-ownership-collapse** — C142 (bootstrap fast-path checks claims only at session
  open; simultaneous empty snapshots produce a thundering-herd of same-file claims).
- **coordinator-amplifies-unseen-defect** — C154 (a four-plus-agent hub-and-spoke window
  ground to a halt while peer-pair sidebars resolved decisions in minutes).
- **completion-language-overload** — C163 (done was repeatedly reset toward the far end:
  implemented, pushed, merged AND live; merge-ready declarations fire on fluent surface
  signals).
- **record-staged-full-index-fingerprint** — C04 (also tier C).
- **repo-wide-autofix-sweep-footgun** — C03 (also tier C).

## Tier E — the owner's manual round (187, ranked)

Ranked by recomputed window spread, then supporting-leaf count, then id. The full ranked list
with pattern text is in the tier-table JSON; the head of the queue:

| Rank | Id | Evidence | Kind |
| --- | --- | --- | --- |
| 1 | C245 | 10w/8L | distributional |
| 2 | C101 | 7w/7L | recurrence |
| 3 | C77 | 6w/7L | recurrence |
| 4 | C243 | 6w/5L | absence |
| 5 | C246 | 6w/5L | distributional |
| 6 | C160 | 5w/5L | trajectory |
| 7 | C161 | 5w/5L | recurrence |
| 8 | C43 | 4w/4L | recurrence |
| 9 | C93 | 4w/4L | recurrence |
| 10 | C109 | 4w/4L | trajectory |
| 11 | C117 | 4w/4L | recurrence |
| 12 | C158 | 4w/4L | recurrence |

Given the validate regime's demonstrated one-directional over-kill, high-spread tier-E entries
(C245's ten distinct windows especially) deserve the first look.

## What flows next

- The rescue set for conservation intake is **tiers A–D: 59 distinct candidates** (26 keeps
  plus 33 rescued kills). Tier A re-finds mostly confirm existing homes; the conservation
  value concentrates in the 8 novel survivors, the 15 C-only rescues, and the 15 D-only
  rescues.
- Routing runs through the standard conservation machinery (capture buffer →
  consolidate-until-done); the buffers are empty and ready.
- Tier E awaits the owner's manual round; nothing in it is deleted — kills are conserved in
  the committed checkpoints.
- The topology redesign (ws2+) is unchanged by this salvage and still gated on its fresh-seat
  readiness review.
