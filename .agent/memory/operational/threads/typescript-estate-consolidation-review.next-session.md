---
fitness_line_target: 240
fitness_line_limit: 420
fitness_char_limit: 28000
fitness_line_length: 100
fitness_content_role: reference
overflow_disposition: 'leave-if-live; else conserve-insight-and-delete — never archive/split/rotate/shard'
merge_class: index-narrative-tables
---

# TypeScript Estate Consolidation Review — Next Session

## Thread Identity

Thread: `typescript-estate-consolidation-review`

Goal: build reproducible, whole-estate evidence for deciding which repeated
TypeScript and TSX structures and algorithms should be generated, shared,
inlined, deleted, rerouted, or deliberately kept; identify the correct semantic
authority and intervention layer for each; and assess which context-independent
responsibilities could later earn promotion into meticulously assured core
packages. The population includes every tracked `.ts` and `.tsx` file,
generated carriers, generators, internal low-level libraries, agent tools,
applications, packages, core workspaces, and the curriculum SDK.

## Participating Agent Identities

| platform | model | session_id_prefix | agent_name | role | first_session | last_session |
| --- | --- | --- | --- | --- | --- | --- |
| codex | GPT-5 | 019fc3 | Lichen guards Phloem | review implementer and documentation-handoff author | 2026-08-02 | 2026-08-03 |
| claude-code | claude-fable-5 | d427b6 | Poppy lifts Bark | repo-architecture lane — adopted claims 95a0678d + 875f1508 at Moment 2 (b2d33b34, succession from Nautilus calls Plankton c6d48b); governing work is the owner's five-point toolkit brief | 2026-08-17 | 2026-08-17 |

## Current Continuation — repo-architecture lane (2026-08-18 morning: #889 landed; support posture)

Seat: Poppy lifts Bark (d427b6), holding claims `95a0678d` (lane) and
`875f1508` (MCP-603 ledger), both live with heartbeats. Owner word
2026-08-18 morning: short-term goal first (MCP-620, Director×Design
plan, ratified ~08:3xZ) with this seat in SUPPORT posture; the
repo-architecture lane stays resume-ready and reopens AFTER that goal
("make sure the repo architecture lane is ready to pick up again after
that").

- **DONE 2026-08-18**: PR #889 MERGED at 09:22:47Z (merge commit
  `daf56ccad`, merge-bot SETTLE-READY; owner word OVN-2 discharged —
  post-incident safety + content-truing riders both honoured; MCP-601
  Done with the landing comment + seven instrument follow-on pointers).
  The round-2 decomposition artefacts are marked HISTORICAL per the
  workspace-basis reground on all three surfaces (synthesis header,
  plan amendment trail 2026-08-18, top-level JSON metadata). The
  mcp-601-census worktree pruned post-merge (branch merged, remote
  auto-deleted). The task-12 recipe (machine-local
  `~/.claude/tasks/session-c6d48b1b/12.json`) is fully discharged.
- **Governing work at pickup**: the owner's five-point toolkit brief,
  verbatim with both refinements in per-user memory node
  `repo-architecture-brief-toolkit-five-points` and on MCP-619
  (In Progress). The survey programme is CANCELLED at owner ruling;
  post-mortem with salvage dispositions at
  `.agent/reports/workspace-taxonomy-landscape-survey/post-mortem-2026-08-17.md`.
- **Delivered baseline**: Oak Toolkit Atlas v1 — repo-canonical at
  `.agent/reports/repo-architecture/oak-toolkit-atlas.html`, published
  artifact URL bound to that file (update = edit file, republish with
  the URL).
- **Resume order at lane reopen**: (1) re-ground per start-right (this
  record is hypothesis, not truth); (2) MCP-619 phase-2 warrant card
  via the Director (foundation-layer design first — designed finishable
  cores, constitutive of the conceptual shape per the owner's
  refinement; the Director re-sequenced the card behind the MCP-620
  short-term goal at owner word); (3) MCP-603 ledger: claim and
  worktree live, but its consumer set changed (the "fresh survey
  design" died with the programme; the change-design/tranche consumer
  remains) — re-derive its warrant at the phase-2 card, never
  self-adjudicate.

## Prior continuation — typescript-estate review execution (2026-08-03; conserved — still-live context for the MCP-603 frozen corpus)

The 2026-08-03 Lichen guards Phloem session is closed by owner direction. The
thread remains active and transfers through this record; no implementation
authority or live evidence should be inferred from the retired seat.

- Branch: `jimcresswell/typescript-estate-consolidation-review` in the dedicated
  worktree whose basename is `typescript-estate-review-019fc3`; resolve its
  host path with `git worktree list`.
- Recorded base: `7ea6ba55b16988cfb0cdcb0f485db32f71777628`; this was not reverified during
  the documentation-only closeout.
- Invocation pointer: `$oak-start-right-team continue
  typescript-estate-consolidation-review from
  .agent/memory/operational/threads/typescript-estate-consolidation-review.next-session.md`.
- Controlling plan: read
  `.agent/plans/delivery/typescript-estate-consolidation-review.plan.md` in the
  dedicated worktree.
- Exact implementation handoff: read
  `.agent/reports/typescript-estate-consolidation-review/handoff-2026-08-03.md`
  in the dedicated worktree before inspecting or changing source.
- Next safe step: after fresh owner authority for implementation and
  validation, preserve and inspect the complete dedicated-worktree diff; then
  cure the known secure-identity integration/type boundary before extending any
  classifier or running any estate extraction.
- Completed prerequisites: the revision-2.6 contract is frozen; the single
  validated-config ingress and classification facade/engine boundary were
  independently approved; focused low-level slices have prior evidence recorded
  in the handoff.
- Recent relevant commits: none for this review. The whole tranche is
  uncommitted local work.
- Team expectation: unknown until live grounding. Do not infer ownership from
  the previous team or edit the primary coordination checkout as an
  implementation surface.
- Suggested team split if a team forms: one owner for secure identity and
  atomic publication; one owner for the auxiliary Git-read semantic port and
  real adapter; read-only test/security reviewers after each coherent slice.
  Assign exact files before work begins.
- Acceptance bar: the plan's complete evidence contract, including a pinned
  census, raw evidence, executable archetype and SDK proofs, candidate closure,
  three defined top-ten lists, second-worktree reproduction, and final drift
  reconciliation. A green low-level slice is not completion.

## Critical State Boundary

The review is **in progress and not safe by the terminal wrap criterion**.
Uncommitted and untracked implementation exists only in the dedicated local
worktree. There is no preservation commit, push, pull request, raw extraction,
evidence document, census, candidate ledger, top-ten result, disposition set,
or proposal set.

The documentation-only handoff performed no source edit and ran no test,
type-check, lint, formatting, dependency, smoke, package, repository, GitHub,
or remote-state check. The facts below are the last observations made before
that boundary and must be re-established rather than inherited as present
truth.

The collaboration claim, conversation, escalation, and commit-queue registries
were not inspected or modified under the same no-check/documentation-only
boundary. The relay watcher was stopped at session close; its final report
recorded no estate finding or decision and no remaining capacity offer. The
next session must re-ground live coordination state before claiming files.

## Last Observed Implementation State

### Strongest complete boundary

The revision-2.6 classification fragment uses one strict AJV-validated and
defensively prepared configuration ingress. `file-classification.ts` is the
only nominal public classification facade;
`file-classification-engine.ts` is fenced as internal; neutral model types
break the dependency cycle; schema-shaped handwritten validation mirrors were
removed while non-schema semantic checks remain local. An emitted-package smoke
is wired into `test:e2e`.

Prior observed evidence: the emitted-boundary smoke passed; focused dependency
analysis covered 99 modules and 303 dependencies with zero violations; focused
classification tests passed; the final code gateway approved the boundary.

### Other completed focused slices

- `utf16-order.ts` centralises exact UTF-16 ordering; its migration previously
  passed ten affected suites and 50 tests with focused type/lint/format evidence.
- Test-doctrine corrections previously passed 12 suites and 79 tests.
- The pure Git invocation and immutable exact-process projection previously
  passed six integration tests with focused lint/format evidence.
- Secure identity's phase-specific unit boundary previously passed 11 tests.
- The auxiliary decision/transition module began Red and then passed three
  unit tests.

### Known incomplete and broken boundary

The last observed package type-check was red:

1. `identity-secure-read.ts` has an unused import of
   `validateIdentityPathObservation` while also reexporting it.
2. Three stale calls in `implementation-identity.integration.test.ts` pass the
   old `MemoryIdentityFileSystem` directly to
   `createSecureIdentityReadPort`, but the reader now requires a phase-specific
   `IdentitySecureFilePort`.

Atomic publication has phase-specific temp/final target types and a separate
result module, but its 16-test green preceded the final model/result split; the
split was not re-proved. The pure auxiliary decision exists, but the semantic
`PinnedBlobReadPort`, real commit-bound Git adapter, production wiring,
integration rewrite, and built smoke do not.

No new validation was run at handoff, so these known diagnostics are not a
claim that no additional issue exists.

## Frozen Contract Identity

Revision 2.6 was frozen at `2026-08-03T00:47:05Z`.

| Contract member | SHA-256 |
| --- | --- |
| `detector-config.json` | `54ace41d941d9fe190b1fd467a3c3ed8aac223953a2e28841b573830e83c7c2d` |
| `detector-config.schema.json` | `cc052b863ff972b7cfaf03dc53098b6081dcaf9f67faf1984b7c8019d28c8016` |
| `raw-extraction.schema.json` | `365ddba4532efbc91d4c6b8a1970c1a62492a806f6f1b60d76e5dac343396ddf` |
| `evidence.schema.json` | `cfcf97f3e5c5ae75849fe5a4c587a3a32253b84624d9fd652bd617ba887a198e` |

The revision corrected provenance precedence: unreadable source with a
generated path/header signal is `generated-declared-unconfirmed`; `unknown` is
reserved for unreadable source with no generated or imported signal. Do not
silently modify accepted bytes or let later implementation invent policy.

## Resume Order

1. Read this record, the governing plan, report README, current handoff, both
   knowledge-safety records, frozen config/schemas, and the complete local diff.
2. Establish live worktree, branch, base, dirty-state, agent claims, and peer
   custody without cleaning, rebasing, or moving implementation into primary.
3. Remove the secure-read unused import; reconcile public reexports; remove or
   migrate the stale integration fake; replace remaining mutable read-log
   assertions with an immutable exact-path projection; re-prove the focused
   identity boundary.
4. Finish and prove atomic publication after its model/result split, including
   a real filesystem smoke for exclusive temp creation, symlink refusal,
   previous-artefact preservation, and atomic replacement.
5. Add the semantic pinned-blob port and structured Git adapter; wire the pure
   auxiliary decision through snapshot capture; replace queue/counter/call-log
   tests with literal Result projections; add the real built smoke.
6. Obtain security and test-doctrine review of those complete boundaries, then
   establish new focused and package-wide evidence. Prior green observations
   are not substitutes.
7. Continue held contract slices in order: module declarations/resolution,
   delivery, graph/ownership, candidate assembly, and raw-document composition.
8. Run the first pinned estate census only after the held list is closed and
   the external built smoke passes. Then complete calibration, history,
   counterfactual, deterministic generation, packed-SDK, candidate, top-ten,
   proposal, reproduction, and drift work from the plan.

## Standing Decisions and Falsifiers

- Generated code is evidence because the repository controls its form; the
  usual remediation locus remains the semantic source or generator.
- The curriculum SDK is first-class and needs a packed isolated-consumer proof;
  declaration shape is insufficient.
- Static similarity, frequency, or co-change cannot ratify extraction.
- Shared implementation and shared semantic authority are different. Several
  local projections may be the correct form of one portable authority.
- Core admission requires context-independent responsibility, a stable
  contract stated without present callers, simpler higher layers, lower total
  change cost, strict boundaries, meticulous documentation, comprehensive
  tests, and future mutation assurance.
- Reject or relocate a proposed core abstraction when it requires domain or
  vendor policy, frequent caller branching, or greater dependency/change cost.
- No production remediation, package extraction, public API change, external
  ticket, or quality gate is authorised by this review.
- Any first census before contract closure and built-smoke proof is invalid.

## Session-Close Knowledge State

The nearby reflection recovered the central shift: the work stopped being an
inventory script and became construction of an instrument whose own low-level
mechanics must meet the same foundations-first standard it will later use to
judge the estate. That shift was valuable because an unreliable complete-looking
report would be worse than an explicitly absent result.

No deep cross-session consolidation is due. The plan remains active; its
architectural framing already has permanent homes; this record and the
dedicated handoff preserve the live execution state. The next session should
not promote any additional doctrine until repository evidence supplies an
independent instance or a decision that the existing cost-of-change-gradient
homes do not already cover.

## Landing Target for the Next Session

Land one coherent, newly evidenced low-level tranche: secure implementation
identity, atomic publication, and pinned auxiliary Git reads integrated behind
their phase-specific ports with real-adapter smokes and specialist review.
Do **not** include an estate census in the same landing target. The census has a
separate trigger: every required contract slice closed and the complete built
entrypoint proved.
