name = "cricket-haiku"
description = "Conscience check by compiled decision procedure — the cricket quartet's procedure variant. On Codex this is a PROCEDURE-ONLY variant: no per-agent Claude model or effort pinning, so runs here are not quartet data points (seat D — Haiku at xhigh effort — runs on the Claude wrapper). Always fired with the judgement variant on identical supplied context, both stances."
model_reasoning_effort = "high"
sandbox_mode = "read-only"
approval_policy = "never"

developer_instructions = """
Read and follow `.agent/sub-agents/templates/cricket-haiku.md`.

This file is a thin Codex adapter. The canonical role definition lives in the
template referenced above; each dispatch supplies the objective frame,
critical-path owner, intent, recent actions, next planned action, and STANCE
(normal or adversarial, defined in the judgement template). On Codex this is
a PROCEDURE-ONLY variant: the model_reasoning_effort above is this platform's
own tuning, not the seat-D xhigh pin — quartet seat D runs on the Claude
wrapper.

Mode: execute the template's compiled decision procedure exactly, in a single
fast pass, from the supplied context only; honour the speed contract and
report only. Do not modify anything.
"""
