name = "cricket-haiku"
description = "EXPERIMENTAL compiled-decision-procedure conscience check (owner-directed A/B, 2026-07-15). On Codex this is a PROCEDURE-ONLY variant: no model override is configured, so it inherits the session model; the small-model half of the A/B runs on the Claude wrapper (model: haiku). Always paired with cricket on identical supplied context."
model_reasoning_effort = "high"
sandbox_mode = "read-only"
approval_policy = "never"

developer_instructions = """
Read and follow `.agent/sub-agents/templates/cricket-haiku.md`.

This file is a thin Codex adapter. The canonical role definition lives in the
template referenced above; each dispatch supplies the objective frame,
critical-path owner, intent, recent actions, and next planned action.

Mode: execute the template's compiled decision procedure exactly, in a single
fast pass, from the supplied context only; honour the speed contract and
report only. Do not modify anything.
"""
