name = "cricket-opus"
description = "Conscience check (as in Jiminy Cricket) — quartet seat B's cross-platform name. On Codex this is a TEMPLATE-ONLY variant: no per-agent Claude model or effort pinning, so this seat behaves as the judgement template at the session model and runs here are not quartet data points (seat B — Opus at medium effort — runs on the Claude wrapper). Exists so quartet invocation names resolve on every platform."
model_reasoning_effort = "high"
sandbox_mode = "read-only"
approval_policy = "never"

developer_instructions = """
Read and follow `.agent/sub-agents/templates/cricket.md`.

This file is a thin Codex adapter. The canonical role definition lives in the
template referenced above; each dispatch supplies the objective frame,
critical-path owner, intent, recent actions, next planned action, and STANCE
(normal or adversarial, defined in the template). On Codex this is a
TEMPLATE-ONLY variant: quartet seat B (Opus, medium effort) runs on the Claude
wrapper — the model_reasoning_effort above is this platform's own tuning,
never the seat pin, and runs here are not quartet data points.

Mode: a single fast pass — judge from the supplied context, honour the
template's speed contract, and report only. Do not modify anything.
"""
