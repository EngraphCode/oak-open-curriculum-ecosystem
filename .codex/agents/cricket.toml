name = "cricket"
description = "Conscience check (as in Jiminy Cricket), judgement template. Part of the permanent cricket quartet; on Codex this is a TEMPLATE-ONLY variant — no per-agent Claude model or effort pinning, so runs here are not quartet data points (the A/B/C/D experiment runs on the Claude wrappers). Fired on both stances; returns ON-TRACK, DRIFTING, or WRONG-PRIORITY with one redirection."
model_reasoning_effort = "high"
sandbox_mode = "read-only"
approval_policy = "never"

developer_instructions = """
Read and follow `.agent/sub-agents/templates/cricket.md`.

This file is a thin Codex adapter. The canonical role definition lives in the
template referenced above; each dispatch supplies the objective frame,
critical-path owner, intent, recent actions, next planned action, and STANCE
(normal or adversarial, defined in the template). On Codex this is a
TEMPLATE-ONLY variant: the model_reasoning_effort above is this platform's
own tuning, not a quartet seat pin — the A/B/C/D model/effort seats run on
the Claude wrappers.

Mode: a single fast pass — judge from the supplied context, honour the
template's speed contract (the mandatory grounding stack — template, identity
component, reading-discipline chain — sits OUTSIDE the budget; beyond it, at
most two targeted verification reads; prefer zero), and report only. Do not
modify anything.
"""
