# UAT run record — 2026-08-03, PR #735 head (local, authed)

Validation evidence for PR #735 (MCP-462 upstream spec alignment). Smoke
subset plus three PR-specific checks, run against a local boot of the PR
head because the Vercel preview was down with a pre-existing
environment defect (see Findings). Two boots were used: a brief no-auth
boot (dual-shape envelope evidence over curl), then the with-auth boot
(`dev:observe`, sinks `["sentry","posthog"]`) through which every tool
row below ran over an authenticated Clerk OAuth session in a real MCP
host (Claude Code, `oak-local-dev`), with the owner completing sign-in.

```text
Oak Curriculum MCP — UAT run record
Target:        http://localhost:3333 (PR #735 head bcdc62373, dev:observe with auth)
Upstream API:  0.7.0 (2026-05-21) from get-changelog-latest
App version:   workspace dev build of bcdc62373
Date (UTC):    2026-08-03
Run by:        Birch holds Seedling (agent, claude-code host) + owner Clerk sign-in
Mode:          smoke subset + PR-specific checks

Section 0  Inventory self-check ......... PASS   (tools: 40  resources: 6  prompts: 0 asserted, -32601)
Section 1  Transport & auth ............. PASS   (1.1 healthz 200; 1.3 401 + WWW-Authenticate resource_metadata; full OAuth completed in host)
Section 2  Orientation .................. PASS   (2.1 model dual shape; 2.2 upstream 0.7.0 recorded)
Section 3  Discovery & browse ........... PASS   (3.1 science ks3: 41 units / 277 lessons, faceted)
Section 4  Search ....................... PASS   (4.1 photosynthesis science ks3: 25 ranked hits, slugs fetchable)
Section 5  Fetch ........................ PASS   (5.2 lesson:photosynthesis full record)
Section 6  Lesson/unit/sequence depth ... PASS   (6.1 lessons-summary complete)
Section 7  Graph tools .................. PASS   (7.2 prior-knowledge depth 1: bounded, anchors echoed, honest edges)
Section 8  EEF evidence ................. N-A    (get-eef-evidence + eef://interpretation are dormant rows in served-surface.ts)
Section 9  Assets & downloads ........... N-A    (not in smoke subset)
Section 10 Resources .................... PASS   (0.2 six resources incl. widget + navigation guidance trio)
Section 11 Prompts ...................... PASS   (prompts/list -32601; no prompts capability)
Section 12 Error & filter behaviour ..... PASS   (12.2 search without scope: -32602 at input layer)
Section 13 MCP App / widget (optional) .. N-A    (non-Apps host)

PR-specific checks (all PASS):
- get-keywords serves the new pagination NOTE, and behaviour matches it live:
  no limit -> exactly 20 keywords (server default); limit 300 -> the full
  science ks1 set (~160, alphabetical, single page).
- get-key-stages-subject-lessons preserves per-unit offset/limit wording
  ("within each unit", "per unit") against upstream's genericised text.
- get-keywords / get-keyword-graph disambiguation pair served: "LIVE keyword
  set ... paginated" vs bounded frequency-ranked graph snapshot.

Findings (severity per release runbook model):
- [P1] Vercel PREVIEW deployment 500s on every route: boot fails with
  "invalid PostHog product-analytics configuration: pseudonym keyring
  failed strict validation". PRE-EXISTING environment defect, not a PR
  defect: first occurrence 2026-07-31T22:18Z (before this PR existed),
  production alpha healthy, and the locally-held keyring value passes the
  same strict validation — the copy stored in the Vercel Preview
  environment is mangled. Disposition: owner re-sets
  POSTHOG_PSEUDONYM_KEYRING (Preview target) per
  docs/operations/environment-variables.md; preview walkthrough re-runs
  after.
- [P3] This guide was internally contradictory on EEF (§8 preamble and
  Appendix A tools row said live; Appendix A resources paragraph and the
  actual served-surface definition say dormant). Trued in this PR.

Response-shape contract: dual content + structuredContent confirmed? YES
  (content[0] summary + content[1] serialised JSON + structuredContent on
  2.1/2.2 over curl; structuredContent rendered in-host for every row).
Verdict: GO on the PR's served surface (no P0/P1 attributable to the PR;
  the P1 is the preview environment and blocks only the preview-hosted
  walkthrough, not the PR content).
```
