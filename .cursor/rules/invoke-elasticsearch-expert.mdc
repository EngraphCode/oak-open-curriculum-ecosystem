---
description: Invoke elasticsearch-expert when changes touch Elasticsearch mappings, queries, analysers, synonyms, ELSER, RRF, reranking, ingest, or Elastic Serverless capabilities.
globs: 'apps/oak-search-cli/**,packages/sdks/oak-search-sdk/**,packages/libs/search-contracts/**'
alwaysApply: false
---

Read and follow `.agent/rules/invoke-elasticsearch-expert.md`.

In Cursor, use the Task tool with `readonly: true` and `subagent_type: elasticsearch-expert`.
