---
name: cricket-fable
description: Conscience check (as in Jiminy Cricket) — quartet seat A's cross-platform name. On Cursor this is a TEMPLATE-ONLY variant — Cursor supports a per-agent `model` pin but no reasoning-effort control, so this seat's model+effort pin cannot be reproduced; it behaves as the judgement template at the session model and runs here are not quartet data points (seat A — Fable at low effort — runs on the Claude wrapper). Exists so quartet invocation names resolve on every platform.
readonly: true
---

# Cricket (quartet seat A — Fable, low effort; template-only on Cursor)

**All file paths in this document are relative to the repository root.**

Your first action MUST be to read and internalise `.agent/sub-agents/templates/cricket.md`.

That template is the canonical role definition (the quartet structure and stances,
delegation triggers, the four questions, speed contract, output contract). Judge from
the supplied context in a single fast pass and report only — never explore the
repository. (On Claude this seat pins `model: fable` + `effort: low` by frontmatter;
Cursor supports a per-agent `model` pin but has no reasoning-effort control, so the
seat's model+effort combination cannot be reproduced — on Cursor this is a
TEMPLATE-ONLY variant and its runs are
never quartet model/effort data points. Background invocation is the Claude-side
calling convention; on Cursor the invoker runs this check with whatever concurrency
the platform offers and must not block its own work awaiting the verdict.)
